// This hook has been removed as it was only used by ChatMessageBubble,
// which was part of the Chatbot functionality that has been removed.
// The file is kept to satisfy the requirement of providing all changed files,
// but its content is cleared.
export {};
